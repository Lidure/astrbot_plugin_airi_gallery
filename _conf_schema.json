{
  "view_command_mode": {
    "type": "string",
    "description": "看图命令触发模式",
    "hint": "no_prefix: 看看xxx；prefix: /看看xxx。仅影响看看、看全部、看看123 这类浏览命令，其余管理命令固定使用 / 前缀。",
    "default": "no_prefix",
    "options": [
      "no_prefix",
      "prefix"
    ]
  },
  "view_multiple_mode": {
    "type": "string",
    "description": "看看多张时的发送模式",
    "hint": "single: 单条消息包含多张图片（默认）；forward: 以合并转发消息发送，避免刷屏。仅在支持合并转发的平台（如 OneBot）上生效，其他平台自动回退为 single。",
    "default": "single",
    "options": [
      "single",
      "forward"
    ]
  },
  "view_multiple_max": {
    "type": "int",
    "description": "看看多张的最大数量",
    "hint": "看看<分类> N 中 N 的最大值，范围 5~10，默认 10。LLM 工具发送数量也受此限制。",
    "default": 10
  },
  "llm_tool_enabled": {
    "type": "bool",
    "description": "启用 LLM 表情包工具",
    "hint": "开启后，LLM 可以在对话中自动调用本插件的图库来发送表情包/图片。需要 AstrBot 已接入 LLM 提供商。",
    "default": false
  },
  "category_aliases": {
    "type": "list",
    "description": "分类昵称映射",
    "hint": "每条格式为「昵称=分类名」，建议去web ui设置",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "view_all_collage_compress": {
    "type": "bool",
    "description": "看全部拼图是否压缩",
    "default": false,
    "hint": "开启后会缩小看全部生成图的整体尺寸，并使用更高 PNG 压缩率；关闭时保持原始清晰度。"
  },
  "view_all_collage_scale": {
    "type": "float",
    "description": "看全部拼图压缩比例",
    "default": 0.85,
    "hint": "仅在启用压缩时生效。范围建议 0.5 到 1.0，数值越小图片越紧凑、文件越小。"
  },
  "use_permission": {
    "type": "bool",
    "description": "是否开启权限控制（针对删除、导入、创建等破坏性命令）",
    "default": false,
    "hint": "开启后，仅 admins 和 whitelist 中的用户可以执行创建、上传、删除、导入等管理命令。"
  },
  "admins": {
    "type": "list",
    "description": "管理员 id 或用户名",
    "hint": "用于放行全部受权限控制的管理命令。示例：q账号、username、群聊或平台返回的 UID；支持多个条目。",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "whitelist": {
    "type": "list",
    "description": "允许的白名单（id 或用户名）",
    "hint": "用于给少数用户临时授权执行管理命令。示例：q账号、username、UID；支持多个条目。",
    "items": {
      "type": "string"
    },
    "default": []
  }
}

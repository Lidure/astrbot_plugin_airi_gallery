{
  "command_mode": {
    "type": "string",
    "description": "看图命令触发模式",
    "hint": "no_prefix: 看看xxx；prefix: /看看xxx。该配置仅影响看看命令，其余命令统一使用 # 前缀，例如 #分类列表、#上传xxx、#删除123、#导入图库。",
    "default": "no_prefix",
    "options": [
      "no_prefix",
      "prefix"
    ]
  },
  "use_permission": {
    "type": "bool",
    "description": "是否开启权限控制（针对删除、导入、创建等破坏性命令）",
    "default": false,
    "hint": "开启后，只有配置在 admins 或 whitelist 中的用户可以执行破坏性命令。"
  },
  "admins": {
    "type": "list",
    "description": "管理员 id 或用户名",
    "hint": "示例：2542219495 或 username；优先匹配用户 id，支持多个条目。",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "whitelist": {
    "type": "list",
    "description": "允许的白名单（id 或用户名）",
    "hint": "示例：2542219495；用于临时授权，支持多个条目。",
    "items": {
      "type": "string"
    },
    "default": []
  }
}

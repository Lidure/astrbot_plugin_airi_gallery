name: Stage 3A lifecycle verified patch

on:
  push:
    branches:
      - refactor/gallery-sync-service

permissions:
  contents: write

jobs:
  patch-lifecycle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: refactor/gallery-sync-service
          fetch-depth: 0

      - name: Apply lifecycle migration
        shell: bash
        run: python scripts/_stage3a_lifecycle_patch.py

      - name: Repair migrated test import
        shell: bash
        run: sed -i '/from concurrent.futures import ThreadPoolExecutor/a from unittest.mock import Mock' tests/test_main_diagnostics.py

      - name: Remove temporary migration tooling
        shell: bash
        run: git rm .github/workflows/_stage3a_lifecycle_patch.yml scripts/_stage3a_lifecycle_patch.py

      - name: Verify migrated working tree
        shell: bash
        run: |
          python -m py_compile gallery_sync.py main.py
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pytest pyyaml quart
          python -m pytest tests -q

      - name: Commit verified lifecycle migration
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: move sync lifecycle to GallerySync"
          git push origin HEAD:refactor/gallery-sync-service
